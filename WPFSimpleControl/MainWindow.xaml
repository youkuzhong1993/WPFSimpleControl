<Window x:Class="WPFSimpleControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:control="clr-namespace:CustomControl;assembly=CustomControl"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Width="80" Height="20" Click="Button_Click"/>
            <control:TimePicker Width="70"/>
        </StackPanel>
        <control:RandomlyPlacedControl x:Name="rpc" Grid.Row="1"/>
        <RichTextBox x:Name="rtb" IsReadOnly="True" Visibility="Collapsed"
                     control:ControlScrollAttachedProperty.LeftMouseButtonScrollEnabled="true" Grid.Row="1"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled" Background="White"/>
    </Grid>
</Window>
